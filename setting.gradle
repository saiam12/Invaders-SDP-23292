dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

toolchainManagement {
    jvm {
        repositories {
            jdkDownload()   // Gradle version must be 8.10+
        }
    }
}
rootProject.name = "TEAMKWAK"